{
  "name": "@ntina/shared",
  "version": "0.0.1",
  "description": "Shared TypeScript types and constants for Ntina",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
